interface:
  display_name: "Mcp Super Server"
  short_description: "Repo-specific patterns and workflows for mcp-super-server"
  default_prompt: "Use the mcp-super-server repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true